
set(KINETIS_SDK_CPU_DRIVERS)

list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_clock.c)

if(KINETIS_SDK_DRIVER_CMT)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_cmt.c)
endif()

if(KINETIS_SDK_DRIVER_ENET)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_enet.c)
endif()

if(KINETIS_SDK_DRIVER_FLEXCAN)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_flexcan.c)
endif()

if(KINETIS_SDK_DRIVER_LMEM_CACHE)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_lmem_cache.c)
endif()

if(KINETIS_SDK_DRIVER_SDHC)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_sdhc.c)
endif()

if(KINETIS_SDK_DRIVER_SDRAMC)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_sdramc.c)
endif()

if(KINETIS_SDK_DRIVER_SYSMPU)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_sysmpu.c)
endif()

if(KINETIS_SDK_DRIVER_TPM)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_tpm.c)
endif()

if(KINETIS_SDK_DRIVER_TSI)
    list(APPEND KINETIS_SDK_CPU_DRIVERS ${CMAKE_CURRENT_LIST_DIR}/fsl_tsi_v4.c)
endif()


target_sources(
        ${TARGET} PRIVATE
        ${KINETIS_SDK_CPU_DRIVERS}
)

target_include_directories(
        ${TARGET}
        PRIVATE include
)
